import React from "react";

const Footer = () => (
  <footer className="footer shell">
    <p>Market data provided by CoinGecko.</p>
    <p>© {new Date().getFullYear()} Crypto Tracker</p>
  </footer>
);

export default Footer;
